<%= semantic_form_for(@room) do |f| %>
  <%= f.inputs %>
  <%= f.input :repositories, :as => :check_boxes, :label => 'Repositories Allowed in this Room' %>
  <%= f.input :item_attributes, :as => :check_boxes, :label => 'Attributes in this Room' %>
  <%= f.actions %>
<% end %>
