%p
  The class request for
  %em= @course.title
  has been uncancelled.

- unless @custom_text.blank?
  = @custom_text.html_safe

%p
  If you have any questions or additional details to share, please add a note to the
  = succeed "." do
    - if @test
      %a{ href: '#' } class request (linked to course)
    - else
      %a{:href => "http://#{site_url}#{course_path(@course)}"} class request
