This is a test email to verify that the Class Request Tool email system is working.

Delayed jobs:
<% if @delayed_jobs.nil? %>
  (No jobs queued)
<% else %>
  There are notifications in the queue.
<% end %>
