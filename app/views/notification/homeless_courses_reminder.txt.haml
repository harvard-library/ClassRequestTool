The following homeless classes have yet to be assigned a location. If any of them are appropriate for your library or archive, please edit the course and assign it.
- @courses.each do |course|
  = render :partial => 'shared/notification_course_info', :locals => { course: course, test: @test  }
  Edit this course: 
  - if @test
    (link to course)
  - else
    #{"http://\#{site_url}\#{edit_course_path(course.id)}"}

- unless @custom_text.blank?
  = strip_tags(@custom_text)
